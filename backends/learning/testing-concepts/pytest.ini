[pytest]
asyncio_mode = auto
addopts    = -v --tb=short
markers    =
    db:      tests that use a real SQLite database
    asyncdb: tests that use an async SQLite database
    slow:    tests that take noticeably longer than average
