[pytest]
asyncio_mode = auto
# --import-mode=importlib gives each test file a unique module name, avoiding
# "import file mismatch" when multiple sections share test_schema.py filenames.
addopts    = -v --tb=short --import-mode=importlib
testpaths  = .
