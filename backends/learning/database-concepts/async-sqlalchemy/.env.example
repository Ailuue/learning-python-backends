# Only needed if your Postgres differs from the default below.
# The shared database-concepts/docker-compose.yml provisions this database with
# user 'postgres' / password 'postgres', and db.py already defaults to this URL —
# so with the standard Docker setup you don't need a .env at all.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/async_pool_demo
